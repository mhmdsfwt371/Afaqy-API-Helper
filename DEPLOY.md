# نشر مساعد الإنتجريشن — دليل القرار والخطوات

---

## اقرا ده الأول — نقطة القرار

الملف ده جواه:

- تركيبة الـ API بالكامل: 534 إندبوينت بمساراتها وأشكال بودياتها
- أسماء عملاء حقيقيين وتفاصيل تذاكرهم (في النسخة الداخلية بس)
- تفاصيل تذاكر ومشاكل داخلية وردود جاهزة
- ملاحظات عن نقاط ضعف في النظام (زي إن الـ API بترجع 200 مع الأخطاء)

**GitHub Pages مفتوح للنت بالكامل**، حتى لو المستودع نفسه private — على الباقات العادية الصفحة نفسها بتفضل عامة. أي حد يعرف اللينك أو يوصله من محرك بحث يقدر يفتح الملف ويقرا كل اللي فوق.

**وبوابة الدخول مش هتحميك**، لأنها بتشتغل بعد ما الملف يكون اتحمّل على جهاز الزائر. أي حد يفتح كود الصفحة يشوف كل حاجة. البوابة بتمنع الفضولي، مش بتمنع اللي بيدوّر.

**التوصية بترتيب واضح:**

| # | الطريقة | الحماية | الجهد | ملاحظة |
|---|---------|---------|-------|---------|
| 1 | سيرفر داخلي على نفس دومين الـ API | كاملة | نص يوم من فريق التقنية | **الأفضل** — وبيحل مشكلة تانية كمان |
| 2 | Cloudflare Pages + Cloudflare Access | كاملة | ساعة | بيقفل الصفحة قبل ما تتحمّل أصلاً |
| 3 | GitHub Pages + بوابة فايربيز | ضعيفة | ساعة | بس لو شيلت الأسماء والتذاكر |

---

## ليه الخيار الأول بيحل مشكلتين مش واحدة

دلوقتي لما تضغط «جرّب» المتصفح بيمنع النداء. السبب إن الصفحة على دومين والـ API على دومين تاني.

لو حطيت الصفحة **على نفس دومين الـ API** — مثلًا `api.afaqy.sa/support/` أو أي مسار داخلي عندكم — المنع ده بيختفي، و«جرّب» و«جرّب وصحّح» و«شغّل كل الخطوات» تشتغل من غير أي عوائق.

يعني قرار الاستضافة الداخلية مش بس بيحمي الداتا، ده كمان بيفتح نص إمكانيات التول اللي حاليًا متعطّلة.

**اللي تطلبه من فريق التقنية:**

> محتاجين نحط ملف HTML واحد على مسار داخلي، ويكون مقفول على شبكة الشركة أو على تسجيل الدخول بتاعنا. الملف ثابت، مفيش سيرفر ولا قاعدة بيانات، والتحديث بيبقى باستبدال الملف.

---

## الخيار التاني: Cloudflare Pages مع Access

الأفضل لو الاستضافة الداخلية هتاخد وقت.

1. اعمل مستودع private على GitHub وحط فيه `afaqy-api-helper.html` باسم `index.html`
2. من Cloudflare Dashboard: **Workers & Pages** ← **Create** ← **Pages** ← **Connect to Git** ← اختار المستودع
3. Build command: سيبها فاضية · Output directory: `/`
4. بعد النشر: **Zero Trust** ← **Access** ← **Applications** ← **Add an application** ← Self-hosted
5. حط دومين الصفحة، وفي **Policies** اعمل سياسة: `Emails ending in @afaqy.com`

كده الصفحة مش بتتحمّل خالص إلا بعد ما كلاودفلير يتأكد من الإيميل. دي حماية حقيقية مش ستارة.

---

## الخيار التالت: GitHub Pages + بوابة فايربيز

**استخدمه بس لو شيلت من الملف:** أسماء العملاء من الأمثلة ومن الأخطاء المعروفة، وأي تفاصيل تذاكر.

### أ) الرفع على GitHub

```bash
mkdir afaqy-support && cd afaqy-support
cp /المسار/afaqy-api-helper.html index.html
git init
git add index.html
git commit -m "Support integration helper"
git branch -M main
git remote add origin https://github.com/<الحساب>/<المستودع>.git
git push -u origin main
```

بعدين من صفحة المستودع: **Settings** ← **Pages** ← Source: `Deploy from a branch` ← Branch: `main` / `root` ← Save.

اللينك بيطلع خلال دقيقتين. بتاعنا: `https://mhmdsfwt371.github.io/Afaqy-API-Helper/`

### ب) إنشاء المشروع على فايربيز

1. افتح `console.firebase.google.com` ← **Add project** ← سمّيه `afaqy-support`
2. **Build** ← **Authentication** ← **Get started** ← فعّل **Email/Password**
3. **Project settings** ← تحت *Your apps* اضغط أيقونة الويب `</>` ← سجّل التطبيق ← هيديك بلوك فيه `apiKey` و `authDomain` و `projectId`
4. **Authentication** ← **Settings** ← **Authorized domains** ← **Add domain** ← حط دومين الصفحة: `<الحساب>.github.io`

### ج) عمل يوزرات للفريق

**Authentication** ← **Users** ← **Add user** ← اكتب الإيميل وكلمة سر مؤقتة ← **Add user**.

كرّرها لكل واحد في الفريق. مفيش تسجيل ذاتي — انت بس اللي بتعمل الحسابات، وده مقصود.

لما حد يمشي من الشركة: نفس الصفحة ← التلات نقط جنب اسمه ← **Delete account**.

### د) تشغيل البوابة في الملف

افتح `index.html` ودوّر على السطر ده قريب من أول الكود:

```js
const AUTH = {
  enabled: false,
```

غيّرها كده:

```js
const AUTH = {
  enabled: true,
  allowedDomain: 'afaqy.com',
  firebase: {
    apiKey: 'الصق القيمة من الخطوة ب-3',
    authDomain: 'afaqy-support.firebaseapp.com',
    projectId: 'afaqy-support'
  }
};
```

ارفع الملف تاني، وخلاص.

> `apiKey` بتاع فايربيز مش سر ومش مشكلة إنه ظاهر — ده تصميم فايربيز نفسه. اللي بيحمي فعلًا هو إنك انت اللي بتعمل الحسابات، وإن الدخول محصور على دومين الشركة.

---

## التحديث بعد النشر

الملف واحد، فالتحديث بسيط:

```bash
cp /المسار/afaqy-api-helper.html index.html
git add index.html && git commit -m "تحديث" && git push
```

**بس في حاجة أهم من ده:** التول فيها فورم «ضيف معلومة جديدة» في تبويب الأساسيات. الفريق بيسجّل فيه اللي بيتعلمه ويصدّره ملف.

**حدّد شخص واحد مسؤول** يجمّع الملفات دي كل أسبوع، يستوردها في نسخته، ويرفع نسخة محدّثة. من غير الشخص ده، التول هتفضل زي ما هي وهتبقى قديمة خلال شهرين.

---

## قايمة تشييك قبل ما تنشر

- [ ] قررت الاستضافة، وعارف إن اللي على GitHub Pages مقروء للكل
- [ ] لو اخترت الاستضافة العامة: شيلت أسماء العملاء من الأمثلة ومن الأخطاء المعروفة
- [ ] جرّبت اللينك من جهاز بره شبكة الشركة وشوفت هل بيفتح من غير دخول
- [ ] عملت حسابات لكل واحد في الفريق، وكل واحد بحسابه هو
- [ ] فهّمت الفريق إن التول بتنادي حسابات عملاء حقيقية، والتجربة بتحصل بضغطة منهم
- [ ] حددت مسؤول تجميع المعرفة الأسبوعي
